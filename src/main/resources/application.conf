# Include secrets configuration if it exists
include "secrets.conf"

vercel {
    apiUrl = "https://api.vercel.com"
    projectName = "v0-toolhub24"
    # Token and teamId are loaded from secrets.conf
}

github {
    owner = "your-github-username"
    repo = "your-repo-name"
    # Token is loaded from secrets.conf
}

git {
    # Author info and token are loaded from secrets.conf
    defaultBranch = "main"
}

app {
    projectRootDir = "/Users/tanglei/projects/v2gc"
    retryAttempts = 3
    retryDelay = 1000
} 